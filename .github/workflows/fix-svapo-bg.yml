name: Corregge sfondo Svapo Store
on:
  push:
    paths:
      - '.github/workflows/fix-svapo-bg.yml'
permissions:
  contents: write
jobs:
  fix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Ripristina JPEG corretto
        shell: bash
        run: |
          base64 -d Sfondosvapostore.b64.txt > Sfondosvapostore.jpg
          test "$(head -c 2 Sfondosvapostore.jpg | xxd -p)" = "ffd8"
          test "$(tail -c 2 Sfondosvapostore.jpg | xxd -p)" = "ffd9"
          file Sfondosvapostore.jpg
      - name: Salva correzione e pulisci file temporanei
        shell: bash
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add Sfondosvapostore.jpg
          git rm .github/workflows/fix-svapo-bg.yml Sfondosvapostore.b64.txt Sfondosvapostore.tmp.txt
          git commit -m 'Corregge sfondo Svapo Store'
          git push
